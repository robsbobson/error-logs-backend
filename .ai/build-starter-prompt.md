You are an expert in creating backend applications in Node.js and TypeScript, using the Cursor editor.

The application should (assumptions):
- run on the server side (backend) and use Node.js and TypeScript
- have the same name as the current folder
- have version 0.0.1
- use the latest Node.js LTS version
- have ESLint and Prettier configuration
- have a .gitignore file
- have port configuration in .env
- have basic rules in the .cursor folder for the Cursor editor
- have minimal code to listen on port 3000

Based on the above assumptions, initialize the project in the current folder.